## 2026-07-08 (KST) — TM_Task_Manager 미구현 기능 분석

### 코드 버전
- 리뷰 커밋: `3663f7dc` (branch `main`)
- 직전 리뷰: 없음 (최초)
- 대상: `src/TM_Robot_Task_Manager/` (PyQt GUI + job_executor + services) + 별도 웹 GUI `/home/amap/Desktop/TRobotics_Client`
- 관련 user_instruction: `## 2026-07-08 14:03 (KST) · sess:93c4cdf4` — "미구현 기능 분석" + "localhost:3000 로봇 연결 안 됨 분석"

### 분석 분기 명시
- 분기: 전체 구조 분석 (디렉토리·다중 모듈) — "미구현 기능" 스코프로 한정
- 감지된 도메인: ros2-review (rclpy, tm_msgs 서비스, launch)

### 대상 범위
Job 실행 엔진(`job_executor.py`, 26 job type), UI 레이어(`main_window.py` + `tabs/`), 서비스 레이어(`services/`), 그리고 사용자가 언급한 웹 GUI `TRobotics_Client`(포트 3000).

---

### 미구현/스텁 인벤토리 — job_executor.py (26 job type 중)

| # | job_type | 메서드 | 상태 | 근거(file:line) |
| --- | --- | --- | --- | --- |
| 1 | move_to_ar_offset | `_exec_move_to_ar_offset` | STUB | job_executor.py:865 `# TODO`, :867 `return True # 임시` |
| 2 | wait_for_detection | `_exec_wait_for_detection` | STUB | :931 `# TODO: Vision 연동`, :933 `return True # 임시` |
| 3 | align_to_ar_tag | `_exec_align_to_ar_tag` | STUB | :1199 `# TODO`, :1202 `pass`, :1204 `return True # 임시` |
| 4 | move_to_ar_center | `_exec_move_to_ar_center` | STUB | :1231 `# TODO`, :1234 `pass`, :1236 `return True # 임시` |
| 5 | vision_process | `_exec_vision_process` | PARTIAL | camera 경로 가짜: :1968 `# TODO`, :1970 "미구현 - 테스트 이미지", :1971 `np.zeros`; `self.runtime_vars` 미초기화(:1989,:2021) AttributeError 잠재 |
| — | (dead code) | `_read_and_store_landmark_result` | 미사용 | def :1759, 호출처 없음 |

정상 구현(참고): go_home, move_to_point, move_linear, line_move_to_point, scan_ar_tag, scan_tm_landmark(+alias), find_landmark, scan_tm_landmark_jig, calculate_plate_pose, generate_runtime, gripper_open/close/home, wait, read/write_digital_io, read_analog_io, align_tm_landmark, measure_point, ai_inspection — 실제 ROS2 서비스/vision_manager/모션 호출 확인됨.

### 미구현/스텁 인벤토리 — UI 레이어

| # | 위젯/핸들러 | 상태 | 근거(file:line) |
| --- | --- | --- | --- |
| 7 | `actionHome` (메뉴 "홈") | 핸들러 미연결 | `_connect_signals` main_window.py:510-543 에 없음; HOME 이동 미구현 주석 main_window.py:304-305 |
| 8 | `pushButton_moveToChessboardCenter` | 핸들러 미연결 | .py 참조 0건 (ui/main_window.ui:1303 정의) |
| 9 | `checkBox_autoSave` (정밀도) | 값 미사용 | precision_test_tab 어디서도 읽지 않음 |
| 10 | `_update_tcp_pose_labels` | `pass` 스텁 | settings_tab.py:288-291 |
| 11 | `_on_ps2_status_changed` | print만(UI 로그 X) | task_edit_tab.py:993-995 |

### 미구현/스텁 인벤토리 — 서비스 레이어

| # | 서비스.메서드 | 상태 | 근거(file:line) |
| --- | --- | --- | --- |
| 12 | robot_connection.`disconnect` | STUB(로컬 플래그만) | robot_connection.py:146-165 |
| 13 | robot_connection.`_on_feedback_state` | 추정 필드 `error_code` | robot_connection.py:76-82 (필드 부재 시 ready 항상 True) |
| 14 | io_control_service.`set_digital_output` | fire-and-forget | io_control_service.py:218-219 (future 미확인 return True) |
| 15 | handeye_test_manager.`_move_to_position` | 고정 `sleep(2.0)` 가짜 완료 | handeye_test_manager.py:339-344 |
| 16 | coordinate_system_manager.`_calculate_relative_pose` | 단순 뺄셈(회전 무시) | coordinate_system_manager.py:678-700 → TF 오류 가능 |

주의(오탐 방지): `coordinate_system_manager.py:406` 및 `ai_detection_service.py:45` 의 하드코딩 경로 `/home/amap/TM_Robot_ros2_ws/...` 는 심볼릭 링크(`/home/amap/TM_Robot_ros2_ws -> 실제 워크스페이스`)가 존재하므로 **현재는 해석됨**(취약하나 미작동 아님).

---

### 별도 웹 GUI — TRobotics_Client (포트 3000)

위치: `/home/amap/Desktop/TRobotics_Client` (이 ROS2 워크스페이스 밖, Vite+React19+MUI+Redux).

- **로봇 연결 코드 전무**: src 전체 `fetch/WebSocket/axios/roslib` 0건. `연결` 버튼 = `console.log("시스템 연결 시도...")` (MainManager.tsx:32). `정지`=console.log, `비상정지`=alert만.
- **IP 필드는 하드코딩 표시용**: TaskEditor.tsx:422 `defaultValue="169.254.183.219"`(구주소), :684 `Status: Disconnected` 리터럴 텍스트.
- **탭 10개 중 3개만 파일 존재**(Task 편집=폼 UI만, Vision/Monitor="준비 중"), 7개는 `ReadyContent` 더미. 탭 파일 7개(AccuracyTest/CoordSetup/GlobalValue/HandEyeTest/IOControl/Joystick/Setting)는 **0 byte 빈 파일**.
- **로그인 우회**: App.tsx `isAuthenticated` 기본 `true`.

---

### 평가

severity 분포: Critical 0 / High 3 / Medium 6 / Low 3 / Info 2
Verdict: COMMENT

**함수 #1·#2·#3·#4 (AR 태그 모션 군집) — [논리][ros2] 실제 모션 미전송 (High)**
   재현: 해당 job 실행 시 `return True # 임시`만, 로봇 미이동인데 성공 보고. job_executor.py:867/933/1204/1236
   권고: ROS2 서비스 호출(set_positions/SendScript) 연결 또는 job 목록에서 비활성/경고 표기

**함수 #5 `_exec_vision_process` — [논리] camera 기본 경로 가짜 + runtime_vars 미초기화 (High)**
   재현: input_source='camera'(기본값) 시 테스트 이미지 생성(job_executor.py:1971); 'variable'/결과저장 경로에서 `self.runtime_vars` AttributeError(:1989,:2021)
   권고: 실제 카메라 캡처 연결, `__init__`에 `self.runtime_vars` 초기화

**웹 GUI 연결 불가 — [논리] 연결 로직 부재 (High)**
   재현: TRobotics_Client `연결` 버튼이 console.log만(MainManager.tsx:32), 네트워크 호출 0건 → 어떤 IP를 입력해도 연결 불가
   권고: 브라우저는 TM 5890/5891 raw TCP 불가 → rosbridge(:9090)+roslibjs 또는 REST/WS 백엔드 브리지 필요. 별도 설계 사안

**함수 #7 `actionHome` / #8 chessboard 이동 — [품질] 미연결 위젯 (Medium)**
   권고: 핸들러 연결 또는 위젯 제거

**함수 #12 `disconnect` / #14 `set_digital_output` / #15 handeye 모션 — [논리] 무확인 성공 보고 (Medium)**
   권고: 실제 완료/응답 확인 후 결과 반환

**함수 #6 dead code / #10 pass 스텁 / #11 print만 — [품질] (Low)**

---

### 자체 점검 결과
- 인벤토리 file:line 근거 100% 명시, severity 분포·Verdict 2줄 기재, 감지 도메인(ros2) 명시.
- 오탐(심볼릭 링크 경로) 별도 표기.
